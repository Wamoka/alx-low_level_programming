#include "holberton.h"
/**
 * main - tests functions that prints if integer is positive or negative
 * positive_or_negative: test if i is 0
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
